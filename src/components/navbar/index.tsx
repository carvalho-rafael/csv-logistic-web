import { Container } from "@material-ui/core";
import { Settings } from "@material-ui/icons";

export default function Navbar() {
  return (
    <Container>
      <header>
        <h1><Settings /> CSV - Logistic</h1>
      </header>
    </Container>
  )
}